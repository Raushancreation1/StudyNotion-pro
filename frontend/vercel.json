{
  "version": 2,
  "installCommand": "npm ci || npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "index.html" }
  ]
}
